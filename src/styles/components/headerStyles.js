import { StyleSheet } from 'react-native';

const headerStyles = StyleSheet.create({
  steamLogo: {
    width: 150,
    height: 60,
    resizeMode: 'contain',
    alignSelf: 'center',
    marginTop: 0,
  },
});

export default headerStyles;
